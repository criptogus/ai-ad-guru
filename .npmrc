
# Use hoisted node_linker for better compatibility
node-linker=hoisted

# Explicitly omit optional dependencies to avoid rollup build issues
omit=optional

# Enable legacy peer deps to fix compatibility issues
legacy-peer-deps=true
