
# Disable optional dependencies to fix rollup build issues
node-linker=hoisted
public-hoist-pattern[]=*rollup*
shamefully-hoist=true

# Completely disable optional dependencies
optional=false

# Enable legacy peer deps to fix compatibility issues
legacy-peer-deps=true
