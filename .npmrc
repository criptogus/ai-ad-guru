
# Skip optional and peer dependencies to avoid native module issues
optional=true
omit=optional
prefer-offline=true
node-linker=hoisted
shamefully-hoist=true
legacy-peer-deps=true
# Ignore missing optional dependencies
ignore-scripts=true
# Avoid platform-specific binaries
platform=neutral
# Force pure JavaScript versions of dependencies
force-pure-lockfile=true
