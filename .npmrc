
# Disable native module compilation
ignore-scripts=true
optional=false
omit=optional
build-from-source=false

# Force using pure JS implementations
platform=neutral
node-linker=hoisted

# Explicitly disable Rollup native modules
ROLLUP_NATIVE_DISABLE=1
DISABLE_NATIVE_MODULES=1
